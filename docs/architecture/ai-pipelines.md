# AI Pipelines

## Document Flow

Upload -\> Parse -\> Chunk -\> Summarize -\> Extract Claims -\> Extract
Entities -\> Build Relations -\> Generate Embeddings -\> Persist -\>
Graph Update -\> Search Index.

## Pipelines

-   Summarization
-   Topic extraction
-   Entity extraction
-   Claim extraction
-   Contradiction detection
-   Embedding generation
-   Citation validation
